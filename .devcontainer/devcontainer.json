{
  "name": "Oracle DB with SQL*Plus",
  "dockerFile": "Dockerfile",
  "context": ".",
  "dockerComposeFile": ["../docker-compose.yml"],
  "service": "sqlplus",
  "extensions": [
    "ms-azuretools.vscode-docker",
  ],
  "postCreateCommand": "docker-compose up -d",
  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash"
  }
}
